# Fixture: ghostty config with `background` + `window-colorspace =
# display-p3`. The hex value must be interpreted as Display P3, not
# the default sRGB, so the chrome tint matches what's rendered inside
# the terminal pane.

window-colorspace = display-p3
background = #0a1f0a
