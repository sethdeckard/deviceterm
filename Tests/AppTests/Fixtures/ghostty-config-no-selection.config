# Fixture: a ghostty config that doesn't set selection-background.
# `GhosttyThemeColors.selectionBackground` should return nil so the
# caller falls back to NSColor.controlAccentColor.

theme = nord
font-size = 13
foreground = #d8dee9
