# Fixture: a ghostty config that explicitly sets the colorspace to
# `srgb` (ghostty's default). Confirms the colorspace reader treats
# the literal `srgb` value the same as an absent key.

window-colorspace = srgb
selection-background = #1e5680
