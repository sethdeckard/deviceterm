# Fixture: ghostty config with a selection-background value that
# isn't a recognized hex color. Helper should return nil rather than
# crash or invent a color, so the caller's fallback engages.

selection-background = cornflowerblue
