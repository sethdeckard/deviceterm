# Fixture: a ghostty config that sets selection-background to a
# hashed hex color. Exercises the happy path for
# `GhosttyThemeColors.selectionBackground`.

theme = catppuccin-mocha
font-size = 14

# Pane drop overlay + focus border should pick this up.
selection-background = #1e5680
selection-foreground = #ffffff
